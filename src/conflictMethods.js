class ConflictMethods {

    //argument: object with {name: [enrolled classes]}
    //returns 
    getConflictGroups = (entries) => {
        for(let key in entries) {
            
        }
    }


}

export default ConflictMethods;